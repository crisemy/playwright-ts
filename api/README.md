# API clients

Add a focused client for a meaningful SUT resource or API area. Clients construct typed requests and return Playwright `APIResponse` values. Keep business-contract assertions in API specs or dedicated assertion helpers.
